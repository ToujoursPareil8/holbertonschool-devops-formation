services:
  web:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./web: /usr/share/nginx/html
    depends_on:
      - api

  api:
    build: ./api/
    ports:
      - "5000:5000"
    depends_on:
      - db
  
  db:
    image: redis:alpine
    ports:
      - "6379:6379"